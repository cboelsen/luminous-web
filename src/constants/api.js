import constants from './index';


const apiConstants = {
    //SERVER_URL: constants.IS_DEV ? `http://localhost:3456/api` : `https://photos.boelsen.net/api`,
    SERVER_URL: constants.IS_DEV ? `http://localhost:3456` : `http://localhost:3456`,
};


export default apiConstants;
